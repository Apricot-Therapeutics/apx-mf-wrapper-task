{
  "manifest_version": "2",
  "task_list": [
    {
      "name": "Convert TIFF to PNG",
      "category": "Image Processing",
      "tags": [
        "Conversion"
      ],
      "executable_non_parallel": "convert_tiff_to_png.py",
      "meta_non_parallel": {
        "cpus_per_task": 1,
        "mem": 4000
      },
      "args_schema_non_parallel": {
        "additionalProperties": false,
        "properties": {
          "zarr_urls": {
            "items": {
              "type": "string"
            },
            "title": "Zarr Urls",
            "type": "array",
            "description": "List of paths or urls to the individual OME-Zarr image to be processed. (standard argument for Fractal tasks, managed by Fractal server)."
          },
          "zarr_dir": {
            "title": "Zarr Dir",
            "type": "string",
            "description": "path of the directory where the new OME-Zarrs will be created. (standard argument for Fractal tasks, managed by Fractal server)."
          },
          "image_dir": {
            "title": "Image Dir",
            "type": "string",
            "description": "Directory where the TIF files are located."
          },
          "batch": {
            "default": true,
            "title": "Batch",
            "type": "boolean",
            "description": "Whether to process the images in batches."
          },
          "batch_size": {
            "default": 200,
            "title": "Batch Size",
            "type": "integer",
            "description": "Number of images to process in each batch. Default is 200."
          }
        },
        "required": [
          "zarr_urls",
          "zarr_dir",
          "image_dir"
        ],
        "type": "object",
        "title": "ConvertTiffToPng"
      },
      "docs_info": "## convert_tiff_to_png\nConvert all TIFF files in a directory to PNG files.\nSee https://github.com/pelkmanslab/mf\n"
    }
  ],
  "has_args_schemas": true,
  "args_schema_version": "pydantic_v2",
  "authors": "Adrian Tschan"
}
